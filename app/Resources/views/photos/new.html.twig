{% extends 'base.html.twig' %}

{% block body %}
    <h1>Photos creation</h1>

    <div class="col-lg-6 col-lg-offset-3">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
        {{ form_end(form) }}

        <ul>
            <li>
                <a class="btn btn-primary" href="{{ path('photos_index') }}">Back to the list</a>
            </li>
        </ul>
    </div>
{% endblock %}
